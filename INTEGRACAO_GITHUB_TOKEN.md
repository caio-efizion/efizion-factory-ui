# Integração Segura e Auditável do GITHUB_TOKEN

> Este arquivo é apenas para consulta interna. O versionamento e push devem ser feitos individualmente neste subprojeto, não na raiz do monorepo.

---

## Objetivo
Garantir que o runner sempre herde o GITHUB_TOKEN do ambiente da API, com logging seguro e auditável, tanto em execução local (host) quanto em Docker.

... (restante do conteúdo igual ao arquivo principal) ...
