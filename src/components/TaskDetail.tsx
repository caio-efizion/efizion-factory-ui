// Este componente depende de react-router-dom e não é compatível com Next.js pages/app.
// Para build Next.js, este arquivo foi isolado. Migre para roteamento Next.js para uso futuro.
export default function TaskDetail() {
  return <div>TaskDetail não disponível neste build. Migrar para Next.js routing.</div>;
}